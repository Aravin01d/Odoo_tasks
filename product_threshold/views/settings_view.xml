<odoo>
    <data>
        <record id="res_config_settings_action" model="ir.actions.act_window">
            <field name="name">Settings</field>
            <field name="res_model">res.config.settings</field>
            <field name="view_mode">form</field>
            <field name="context">{'module' : 'sale'}</field>
        </record>

        <record id="res_config_settings_view_form" model="ir.ui.view">
            <field name="name">res.config.settings.view.form</field>
            <field name="model">res.config.settings</field>
            <field name="priority" eval="15"/>
            <field name="inherit_id" ref="sale.res_config_settings_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//block[@name='catalog_setting_container']" position="inside">
                            <setting string="Product Threshold"
                                help="Set the threshold for products"
                                id="products_threhold_id">
                           <field name="products_threshold"/>
                           <div class="content-group"
                                invisible="not products_threshold"
                                id="group_products_threshold_setting">
                               <div class="text-warning mt16">
                                   <strong>Save</strong>
                                   this page and come back here to set up the
                                   feature.
                               </div>
                           </div>
                       </setting>
                </xpath>
            </field>
        </record>
    </data>
</odoo>